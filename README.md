# 2026-IFCT095PO-GR1-flask

Pequeña API en flask:
¡Por fin llegamos a proyectos mas complejos!

## Setup

Creamos una carpeta para el proyecto (ej: ```C:\python\2026-IFCT095PO-GR1-flask```)
Navegamos a la carpeta y en ella:

Creamos el entorno virtual
> Llamamos al entorno ```.venv``` Este nombre puede ser el que queramos, pero este es tradicional

```bash
python -m venv .venv
```

## Uso

Activamos el entorno virtual

```bash
.venv\Scrips\activate
```
